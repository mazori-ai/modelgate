apiVersion: v1
kind: Service
metadata:
  name: {{ include "modelgate.fullname" . }}
  labels:
    {{- include "modelgate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "modelgate.selectorLabels" . | nindent 4 }}

